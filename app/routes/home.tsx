import { Check, Copy, Download, FileJson, Import, Link2, Plus, Radio, Send, ShieldCheck, Trash2, WalletCards } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import type { Route } from "./+types/home";
import { Badge } from "../components/ui/badge";
import { Button } from "../components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "../components/ui/card";
import { Input } from "../components/ui/input";
import { Label } from "../components/ui/label";
import { Textarea } from "../components/ui/textarea";

type Network = "mainnet" | "preprod" | "preview";
type Mode = "import" | "create";

type Signer = { id: string; label: string; keyHash: string; source?: "payment" | "stake" | "manual" };
type NativeScript = { type: string; keyHash?: string; scripts?: NativeScript[]; required?: number; slot?: number; [key: string]: unknown };
type MultisigWallet = {
  id: string; name: string; network: Network; threshold: number; signers: Signer[];
  paymentScript: NativeScript; stakeScript?: NativeScript | null; script: NativeScript; createdAt: string; imported: boolean;
};
type ParsedScript = { script: NativeScript | null; error: string | null };
type CardanoWalletApi = {
  getUsedAddresses(): Promise<string[]>; getUnusedAddresses(): Promise<string[]>; getChangeAddress(): Promise<string>; getNetworkId(): Promise<number>;
  signTx(txCbor: string, partialSign?: boolean): Promise<string>; submitTx?(txCbor: string): Promise<string>;
};
type WalletProvider = { id: string; name: string; icon?: string; enable(): Promise<CardanoWalletApi> };
type ConnectedWallet = { id: string; name: string; api: CardanoWalletApi; networkId: number; addressHex: string; keyHash: string | null };
type SignatureRecord = { signerKeyHash: string; signerName: string; walletName: string; witnessCbor: string; signedAt: string };
type TxDraft = {
  id: string; title: string; walletName: string; network: Network; recipient: string; lovelace: string; note: string;
  unsignedTxCbor: string; requiredSignatures: number; signerKeyHashes: string[]; signatures: SignatureRecord[]; createdAt: string;
};
type ProviderSettings = { kupoUrl: string; ogmiosUrl: string; nodeSubmitUrl: string };

declare global { interface Window { cardano?: Record<string, { name?: string; icon?: string; enable?: () => Promise<CardanoWalletApi> }> } }

const STORAGE_KEY = "cardano-multisig.wallets.v2";
const TX_STORAGE_KEY = "cardano-multisig.transactions.v1";
const PROVIDER_STORAGE_KEY = "cardano-multisig.providers.v1";
const LEGACY_STORAGE_KEY = "cardano-multisig.wallets.v1";
const NETWORKS: Network[] = ["mainnet", "preprod", "preview"];
const SAMPLE_PAYMENT_SCRIPT = `{
  "type": "atLeast",
  "required": 2,
  "scripts": [
    { "type": "sig", "keyHash": "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa" },
    { "type": "sig", "keyHash": "bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb" },
    { "type": "sig", "keyHash": "cccccccccccccccccccccccccccccccccccccccccccccccccccccccc" }
  ]
}`;
const SAMPLE_STAKE_SCRIPT = `{
  "type": "all",
  "scripts": [
    { "type": "sig", "keyHash": "dddddddddddddddddddddddddddddddddddddddddddddddddddddddd" }
  ]
}`;

export function meta({}: Route.MetaArgs) {
  return [{ title: "Cardano Multisig" }, { name: "description", content: "Collaborative Cardano native-script multisig signing room" }];
}

const nowIso = () => new Date().toISOString();
function createId(prefix = "id") { const random = typeof crypto !== "undefined" && "randomUUID" in crypto ? crypto.randomUUID().slice(0, 8) : Math.random().toString(36).slice(2, 10); return `${prefix}_${random}`; }
function emptySigner(label = "Signer"): Signer { return { id: createId("signer"), label, keyHash: "", source: "manual" }; }
function isKeyHash(value: string) { return /^[0-9a-fA-F]{56}$/.test(value.trim()); }
function isRecord(value: unknown): value is Record<string, unknown> { return typeof value === "object" && value !== null && !Array.isArray(value); }
function cleanSigner(signer: Signer): Signer { return { ...signer, label: signer.label.trim() || "Unnamed signer", keyHash: signer.keyHash.trim().toLowerCase() }; }
function parseScript(value: string, required: boolean): ParsedScript {
  const trimmed = value.trim();
  if (!trimmed) return required ? { script: null, error: "Script JSON is required." } : { script: null, error: null };
  try { const parsed = JSON.parse(trimmed) as unknown; if (!isRecord(parsed) || typeof parsed.type !== "string") return { script: null, error: "Script must be a JSON object with a type field." }; return { script: parsed as NativeScript, error: null }; }
  catch (error) { return { script: null, error: error instanceof Error ? error.message : "Invalid JSON." }; }
}
function buildNativeScript(signers: Signer[], threshold: number): NativeScript {
  const sigScripts = signers.map((signer) => ({ type: "sig", keyHash: signer.keyHash }));
  if (threshold <= 1) return { type: "any", scripts: sigScripts };
  if (threshold >= sigScripts.length) return { type: "all", scripts: sigScripts };
  return { type: "atLeast", required: threshold, scripts: sigScripts };
}
function countLeafScripts(script: NativeScript | null): number { if (!script) return 0; if (script.type === "sig") return 1; return Array.isArray(script.scripts) ? script.scripts.reduce((total, child) => total + countLeafScripts(child), 0) : 0; }
function requiredSignatures(script: NativeScript | null): number {
  if (!script) return 0; const children = Array.isArray(script.scripts) ? script.scripts : [];
  if (script.type === "sig") return 1; if (script.type === "any") return children.length ? 1 : 0;
  if (script.type === "all") return children.reduce((total, child) => total + requiredSignatures(child), 0);
  if (script.type === "atLeast") return Number(script.required || 0);
  return children.reduce((total, child) => Math.max(total, requiredSignatures(child)), 0);
}
function summarizeScript(script: NativeScript | null) { if (!script) return "Not provided"; const leaves = countLeafScripts(script); if (script.type === "sig") return "1-of-1"; if (script.type === "any") return `1-of-${leaves}`; if (script.type === "all") return `${leaves}-of-${leaves}`; if (script.type === "atLeast") return `${script.required ?? 0}-of-${leaves}`; return `${script.type} script`; }
function collectSigners(script: NativeScript | null, source: "payment" | "stake") {
  const signers: Signer[] = [];
  function visit(node: NativeScript | null) { if (!node) return; if (node.type === "sig" && typeof node.keyHash === "string" && isKeyHash(node.keyHash)) signers.push({ id: createId(source), label: `${source === "payment" ? "Payment" : "Stake"} signer ${signers.length + 1}`, keyHash: node.keyHash.toLowerCase(), source }); if (Array.isArray(node.scripts)) node.scripts.forEach(visit); }
  visit(script); return signers;
}
function uniqueSigners(signers: Signer[]) { const seen = new Map<string, Signer>(); for (const signer of signers) if (!seen.has(signer.keyHash)) seen.set(signer.keyHash, signer); return [...seen.values()]; }
function migrateWallet(raw: unknown): MultisigWallet | null {
  if (!isRecord(raw) || typeof raw.name !== "string" || !isRecord(raw.script)) return null; const script = raw.script as NativeScript;
  const signers = Array.isArray(raw.signers) ? (raw.signers as Signer[]).filter((signer) => isKeyHash(signer.keyHash)) : collectSigners(script, "payment");
  return { id: typeof raw.id === "string" ? raw.id : createId("wallet"), name: raw.name, network: NETWORKS.includes(raw.network as Network) ? raw.network as Network : "mainnet", threshold: typeof raw.threshold === "number" ? raw.threshold : requiredSignatures(script), signers, paymentScript: isRecord(raw.paymentScript) ? raw.paymentScript as NativeScript : script, stakeScript: isRecord(raw.stakeScript) ? raw.stakeScript as NativeScript : null, script, createdAt: typeof raw.createdAt === "string" ? raw.createdAt : nowIso(), imported: Boolean(raw.imported) };
}
function readJsonArray<T>(key: string, migrate: (raw: unknown) => T | null): T[] { if (typeof window === "undefined") return []; try { const stored = window.localStorage.getItem(key); if (!stored) return []; const parsed = JSON.parse(stored) as unknown; if (!Array.isArray(parsed)) return []; return parsed.map(migrate).filter((item): item is T => Boolean(item)); } catch { return []; } }
function loadWallets(): MultisigWallet[] { const current = readJsonArray(STORAGE_KEY, migrateWallet); return current.length ? current : readJsonArray(LEGACY_STORAGE_KEY, migrateWallet); }
function saveWallets(wallets: MultisigWallet[]) { if (typeof window !== "undefined") window.localStorage.setItem(STORAGE_KEY, JSON.stringify(wallets, null, 2)); }
function migrateDraft(raw: unknown): TxDraft | null { if (!isRecord(raw) || typeof raw.id !== "string" || typeof raw.title !== "string") return null; return { id: raw.id, title: raw.title, walletName: String(raw.walletName || "Wallet"), network: NETWORKS.includes(raw.network as Network) ? raw.network as Network : "mainnet", recipient: String(raw.recipient || ""), lovelace: String(raw.lovelace || ""), note: String(raw.note || ""), unsignedTxCbor: String(raw.unsignedTxCbor || ""), requiredSignatures: Number(raw.requiredSignatures || 1), signerKeyHashes: Array.isArray(raw.signerKeyHashes) ? raw.signerKeyHashes.map(String).filter(isKeyHash).map((x) => x.toLowerCase()) : [], signatures: Array.isArray(raw.signatures) ? raw.signatures as SignatureRecord[] : [], createdAt: String(raw.createdAt || nowIso()) }; }
function loadDrafts() { return readJsonArray(TX_STORAGE_KEY, migrateDraft); }
function saveDrafts(drafts: TxDraft[]) { if (typeof window !== "undefined") window.localStorage.setItem(TX_STORAGE_KEY, JSON.stringify(drafts, null, 2)); }
function loadProviders(): ProviderSettings { if (typeof window === "undefined") return { kupoUrl: "", ogmiosUrl: "", nodeSubmitUrl: "" }; try { return { kupoUrl: "", ogmiosUrl: "", nodeSubmitUrl: "", ...JSON.parse(window.localStorage.getItem(PROVIDER_STORAGE_KEY) || "{}") }; } catch { return { kupoUrl: "", ogmiosUrl: "", nodeSubmitUrl: "" }; } }
function saveProviders(settings: ProviderSettings) { if (typeof window !== "undefined") window.localStorage.setItem(PROVIDER_STORAGE_KEY, JSON.stringify(settings, null, 2)); }
function slugify(value: string) { return value.trim().toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "") || "cardano-multisig"; }
function downloadJson(name: string, value: unknown) { const blob = new Blob([JSON.stringify(value, null, 2)], { type: "application/json" }); const url = URL.createObjectURL(blob); const anchor = document.createElement("a"); anchor.href = url; anchor.download = name; anchor.click(); URL.revokeObjectURL(url); }
function encodeInvite(draft: TxDraft) { const payload = JSON.stringify({ type: "cardano-multisig-invite", version: 1, draft: { ...draft, signatures: [] } }); return btoa(unescape(encodeURIComponent(payload))).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, ""); }
function decodeInvite(value: string): TxDraft | null { try { const normalized = value.replace(/-/g, "+").replace(/_/g, "/"); const json = decodeURIComponent(escape(atob(normalized))); const parsed = JSON.parse(json) as unknown; if (!isRecord(parsed) || parsed.type !== "cardano-multisig-invite" || !isRecord(parsed.draft)) return null; return migrateDraft(parsed.draft); } catch { return null; } }
function hexToBytes(hex: string) { const out = new Uint8Array(hex.length / 2); for (let i = 0; i < out.length; i += 1) out[i] = Number.parseInt(hex.slice(i * 2, i * 2 + 2), 16); return out; }
async function keyHashFromAddress(addressHex: string): Promise<string | null> {
  try {
    const CSL = await import("@emurgo/cardano-serialization-lib-browser"); const address = CSL.Address.from_bytes(hexToBytes(addressHex));
    const base = CSL.BaseAddress.from_address(address); const enterprise = CSL.EnterpriseAddress.from_address(address); const reward = CSL.RewardAddress.from_address(address);
    const credential = base?.payment_cred() ?? enterprise?.payment_cred() ?? reward?.payment_cred(); const keyHash = credential?.to_keyhash();
    return keyHash ? keyHash.to_hex() : null;
  } catch { return null; }
}
function networkLabel(networkId: number) { return networkId === 1 ? "mainnet" : networkId === 0 ? "testnet" : `network ${networkId}`; }
function installedWallets(): WalletProvider[] { if (typeof window === "undefined" || !window.cardano) return []; return Object.entries(window.cardano).filter(([, wallet]) => typeof wallet.enable === "function").map(([id, wallet]) => ({ id, name: wallet.name || id, icon: wallet.icon, enable: wallet.enable!.bind(wallet) })); }
function signatureCount(draft: TxDraft) { const signed = new Set(draft.signatures.map((sig) => sig.signerKeyHash.toLowerCase())); return draft.signerKeyHashes.filter((hash) => signed.has(hash.toLowerCase())).length; }

function ScriptPreview({ title, script }: { title: string; script: NativeScript | null }) {
  return <div className="rounded-lg border border-border bg-slate-950/80"><div className="flex items-center justify-between border-b border-border px-4 py-3"><span className="text-sm font-medium text-slate-200">{title}</span><Badge variant="outline">{summarizeScript(script)}</Badge></div><pre className="code-scroll max-h-80 overflow-auto p-4 text-xs leading-5 text-slate-300"><code>{script ? JSON.stringify(script, null, 2) : "// Paste script JSON to preview"}</code></pre></div>;
}

export default function Home() {
  const [wallets, setWallets] = useState<MultisigWallet[]>([]); const [drafts, setDrafts] = useState<TxDraft[]>([]); const [providers, setProviders] = useState<WalletProvider[]>([]); const [connected, setConnected] = useState<ConnectedWallet | null>(null); const [providerSettings, setProviderSettings] = useState<ProviderSettings>({ kupoUrl: "", ogmiosUrl: "", nodeSubmitUrl: "" });
  const [mode, setMode] = useState<Mode>("import"); const [name, setName] = useState("Team treasury"); const [network, setNetwork] = useState<Network>("mainnet"); const [threshold, setThreshold] = useState(2); const [signers, setSigners] = useState<Signer[]>([emptySigner("Signer 1"), emptySigner("Signer 2"), emptySigner("Signer 3")]);
  const [importName, setImportName] = useState("Existing treasury"); const [importNetwork, setImportNetwork] = useState<Network>("mainnet"); const [paymentScriptText, setPaymentScriptText] = useState(""); const [stakeScriptText, setStakeScriptText] = useState(""); const [copied, setCopied] = useState(false);
  const [txTitle, setTxTitle] = useState("Treasury payment"); const [txRecipient, setTxRecipient] = useState(""); const [txLovelace, setTxLovelace] = useState("2000000"); const [txCbor, setTxCbor] = useState(""); const [txNote, setTxNote] = useState(""); const [activeDraftId, setActiveDraftId] = useState<string | null>(null); const [signaturePackage, setSignaturePackage] = useState(""); const [status, setStatus] = useState("");

  useEffect(() => { setWallets(loadWallets()); setDrafts(loadDrafts()); setProviderSettings(loadProviders()); setProviders(installedWallets()); const invite = new URLSearchParams(window.location.hash.replace(/^#/, "")).get("invite"); if (invite) { const draft = decodeInvite(invite); if (draft) { setDrafts((current) => current.some((item) => item.id === draft.id) ? current : [draft, ...current]); setActiveDraftId(draft.id); setStatus("Invite loaded. Connect the signer wallet to sign this transaction."); } } }, []);
  useEffect(() => saveWallets(wallets), [wallets]); useEffect(() => saveDrafts(drafts), [drafts]); useEffect(() => saveProviders(providerSettings), [providerSettings]);

  const cleanedSigners = useMemo(() => signers.map(cleanSigner), [signers]); const validSigners = cleanedSigners.filter((signer) => isKeyHash(signer.keyHash)); const clampedThreshold = Math.max(1, Math.min(threshold, validSigners.length || 1)); const draftScript = useMemo(() => buildNativeScript(validSigners, clampedThreshold), [validSigners, clampedThreshold]); const canSave = name.trim().length > 0 && validSigners.length >= 2 && clampedThreshold <= validSigners.length; const scriptJson = JSON.stringify(draftScript, null, 2);
  const parsedPayment = useMemo(() => parseScript(paymentScriptText, true), [paymentScriptText]); const parsedStake = useMemo(() => parseScript(stakeScriptText, false), [stakeScriptText]); const importedSigners = useMemo(() => uniqueSigners([...collectSigners(parsedPayment.script, "payment"), ...collectSigners(parsedStake.script, "stake")]), [parsedPayment.script, parsedStake.script]); const importThreshold = requiredSignatures(parsedPayment.script); const canImport = importName.trim().length > 0 && Boolean(parsedPayment.script) && !parsedPayment.error && !parsedStake.error;
  const activeDraft = drafts.find((draft) => draft.id === activeDraftId) ?? drafts[0] ?? null; const selectedSignerHashes = (mode === "import" ? importedSigners : validSigners).map((signer) => signer.keyHash); const selectedRequired = mode === "import" ? Math.max(importThreshold, 1) : clampedThreshold;

  async function connectWallet(provider: WalletProvider) { setStatus(`Connecting ${provider.name}...`); const api = await provider.enable(); const networkId = await api.getNetworkId(); const used = await api.getUsedAddresses(); const unused = used.length ? [] : await api.getUnusedAddresses(); const addressHex = used[0] || unused[0] || await api.getChangeAddress(); const keyHash = await keyHashFromAddress(addressHex); setConnected({ id: provider.id, name: provider.name, api, networkId, addressHex, keyHash }); setStatus(keyHash ? `Connected ${provider.name}. Signer key hash detected.` : `Connected ${provider.name}. Could not derive key hash from wallet address.`); }
  function importWallet() { if (!canImport || !parsedPayment.script) return; const wallet: MultisigWallet = { id: createId("wallet"), name: importName.trim(), network: importNetwork, threshold: importThreshold, signers: importedSigners, paymentScript: parsedPayment.script, stakeScript: parsedStake.script, script: parsedPayment.script, createdAt: nowIso(), imported: true }; setWallets((current) => [wallet, ...current]); }
  function saveCreatedWallet() { if (!canSave) return; const wallet: MultisigWallet = { id: createId("wallet"), name: name.trim(), network, threshold: clampedThreshold, signers: validSigners, paymentScript: draftScript, stakeScript: null, script: draftScript, createdAt: nowIso(), imported: false }; setWallets((current) => [wallet, ...current]); }
  async function copyScript() { await navigator.clipboard.writeText(scriptJson); setCopied(true); window.setTimeout(() => setCopied(false), 1600); }
  function createDraft() { const hashes = uniqueSigners(selectedSignerHashes.map((keyHash, index) => ({ id: createId("txsigner"), label: `Signer ${index + 1}`, keyHash }))).map((signer) => signer.keyHash); const draft: TxDraft = { id: createId("tx"), title: txTitle.trim() || "Transaction", walletName: mode === "import" ? importName : name, network: mode === "import" ? importNetwork : network, recipient: txRecipient.trim(), lovelace: txLovelace.trim(), note: txNote.trim(), unsignedTxCbor: txCbor.trim(), requiredSignatures: Math.min(Math.max(selectedRequired, 1), Math.max(hashes.length, 1)), signerKeyHashes: hashes, signatures: [], createdAt: nowIso() }; setDrafts((current) => [draft, ...current]); setActiveDraftId(draft.id); setStatus("Transaction room created. Copy the invite link and send it privately to signers."); }
  async function copyInvite(draft: TxDraft) { const link = `${window.location.origin}${window.location.pathname}#invite=${encodeInvite(draft)}`; await navigator.clipboard.writeText(link); setStatus("Invite link copied. Send it privately to the intended signers only."); }
  async function signActiveDraft() { if (!activeDraft || !connected) return; if (!activeDraft.unsignedTxCbor.trim()) { setStatus("Paste an unsigned transaction CBOR before signing."); return; } const signerKeyHash = connected.keyHash?.toLowerCase() || "unknown"; const witnessCbor = await connected.api.signTx(activeDraft.unsignedTxCbor.trim(), true); const signature: SignatureRecord = { signerKeyHash, signerName: signerKeyHash === "unknown" ? connected.name : signerKeyHash, walletName: connected.name, witnessCbor, signedAt: nowIso() }; setDrafts((current) => current.map((draft) => draft.id === activeDraft.id ? { ...draft, signatures: [...draft.signatures.filter((sig) => sig.signerKeyHash !== signerKeyHash), signature] } : draft)); setSignaturePackage(JSON.stringify({ type: "cardano-multisig-signature", version: 1, draftId: activeDraft.id, signature }, null, 2)); setStatus("Partial witness captured. Send the signature package back to the coordinator or keep collecting locally."); }
  function importSignature() { try { const parsed = JSON.parse(signaturePackage) as unknown; if (!isRecord(parsed) || parsed.type !== "cardano-multisig-signature" || !isRecord(parsed.signature) || typeof parsed.draftId !== "string") throw new Error("Invalid signature package"); const signature = parsed.signature as SignatureRecord; setDrafts((current) => current.map((draft) => draft.id === parsed.draftId ? { ...draft, signatures: [...draft.signatures.filter((sig) => sig.signerKeyHash !== signature.signerKeyHash), signature] } : draft)); setActiveDraftId(parsed.draftId); setStatus("Signature package imported."); } catch (error) { setStatus(error instanceof Error ? error.message : "Invalid signature package"); } }
  async function copySignaturePackage() { if (!signaturePackage.trim()) return; await navigator.clipboard.writeText(signaturePackage); setStatus("Signature package copied."); }

  return <main className="mx-auto flex w-full max-w-7xl flex-col gap-6 px-4 py-8 text-slate-100 sm:px-6 lg:px-8">
    <section className="grid gap-6 lg:grid-cols-[1fr_360px] lg:items-end"><div className="space-y-5"><Badge variant="outline" className="border-sky-400/30 bg-sky-400/10 text-sky-200">Cardano native scripts</Badge><div className="space-y-4"><h1 className="max-w-4xl text-5xl font-semibold tracking-[-0.08em] text-slate-50 sm:text-7xl lg:text-8xl">Multisig control room.</h1><p className="max-w-2xl text-base leading-7 text-slate-400 sm:text-lg">Import scripts, connect a CIP-30 wallet, create signing rooms, send private invite links, and track who has signed.</p></div></div><Card className="glass-panel overflow-hidden"><CardContent className="grid grid-cols-3 gap-3 p-5"><div className="rounded-lg border border-border bg-slate-950/60 p-3"><div className="text-2xl font-semibold text-sky-200">{wallets.length}</div><div className="text-xs text-slate-400">wallets</div></div><div className="rounded-lg border border-border bg-slate-950/60 p-3"><div className="text-2xl font-semibold text-sky-200">{drafts.length}</div><div className="text-xs text-slate-400">tx rooms</div></div><div className="rounded-lg border border-border bg-slate-950/60 p-3"><div className="text-2xl font-semibold text-sky-200">{connected ? "on" : "off"}</div><div className="text-xs text-slate-400">wallet</div></div></CardContent></Card></section>

    <Card className="glass-panel"><CardHeader><CardTitle className="flex items-center gap-2"><WalletCards className="size-5 text-sky-300" /> Wallet connection</CardTitle><CardDescription>Connect any browser wallet that exposes CIP-30. We detect the payment key hash when possible to match signer status.</CardDescription></CardHeader><CardContent className="space-y-4"><div className="flex flex-wrap gap-2">{providers.length ? providers.map((provider) => <Button key={provider.id} variant={connected?.id === provider.id ? "default" : "secondary"} onClick={() => connectWallet(provider)}>{provider.icon ? <img alt="" className="size-4" src={provider.icon} /> : null}{provider.name}</Button>) : <div className="rounded-lg border border-dashed border-border p-4 text-sm text-slate-400">No CIP-30 wallet detected in this browser.</div>}</div>{connected ? <div className="grid gap-3 rounded-lg border border-border bg-slate-950/60 p-4 text-sm sm:grid-cols-3"><div><div className="text-slate-400">Wallet</div><div className="font-medium">{connected.name}</div></div><div><div className="text-slate-400">Network</div><div className="font-medium">{networkLabel(connected.networkId)}</div></div><div><div className="text-slate-400">Payment key hash</div><div className="truncate font-mono text-xs">{connected.keyHash || "not detected"}</div></div></div> : null}</CardContent></Card>

    <section className="grid gap-6 lg:grid-cols-[minmax(0,1.05fr)_minmax(380px,0.95fr)]"><Card className="glass-panel"><CardHeader><div className="flex flex-wrap items-center justify-between gap-3"><div><CardTitle>Wallet workspace</CardTitle><CardDescription>Import an existing wallet or create a new M-of-N policy.</CardDescription></div><div className="grid grid-cols-2 rounded-lg border border-border bg-slate-950/70 p-1">{(["import", "create"] as Mode[]).map((item) => <Button key={item} variant={mode === item ? "default" : "ghost"} size="sm" onClick={() => setMode(item)}>{item === "import" ? <Import className="size-4" /> : <Plus className="size-4" />}{item}</Button>)}</div></div></CardHeader>{mode === "import" ? <CardContent className="space-y-5"><div className="grid gap-4 sm:grid-cols-[1fr_180px]"><div className="space-y-2"><Label>Wallet name</Label><Input value={importName} onChange={(event) => setImportName(event.target.value)} /></div><div className="space-y-2"><Label>Network</Label><select value={importNetwork} onChange={(event) => setImportNetwork(event.target.value as Network)} className="h-10 w-full rounded-md border border-input bg-slate-950/60 px-3 text-sm text-slate-100 outline-none">{NETWORKS.map((item) => <option key={item} value={item}>{item}</option>)}</select></div></div><div className="space-y-2"><div className="flex items-center justify-between"><Label>Payment script JSON</Label><Button type="button" variant="ghost" size="sm" onClick={() => setPaymentScriptText(SAMPLE_PAYMENT_SCRIPT)}>Load sample</Button></div><Textarea value={paymentScriptText} onChange={(event) => setPaymentScriptText(event.target.value)} placeholder="Paste the existing payment native script JSON" className="min-h-52 font-mono text-xs" aria-invalid={Boolean(parsedPayment.error)} />{parsedPayment.error ? <p className="text-sm text-red-300">{parsedPayment.error}</p> : null}</div><div className="space-y-2"><div className="flex items-center justify-between"><Label>Stake script JSON</Label><Button type="button" variant="ghost" size="sm" onClick={() => setStakeScriptText(SAMPLE_STAKE_SCRIPT)}>Load sample</Button></div><Textarea value={stakeScriptText} onChange={(event) => setStakeScriptText(event.target.value)} placeholder="Paste stake native script JSON if the existing wallet has one" className="min-h-40 font-mono text-xs" aria-invalid={Boolean(parsedStake.error)} />{parsedStake.error ? <p className="text-sm text-red-300">{parsedStake.error}</p> : null}</div><div className="rounded-lg border border-sky-400/20 bg-sky-400/10 p-4 text-sm leading-6 text-sky-100"><div className="mb-1 flex items-center gap-2 font-medium"><ShieldCheck className="size-4" /> Import safety</div>Importing preserves script JSON for verification. It does not prove fund control; verify the address and run a dust transaction before moving value.</div><Button disabled={!canImport} onClick={importWallet} className="w-full"><Import className="size-4" /> Import existing wallet</Button></CardContent> : <CardContent className="space-y-5"><div className="grid gap-4 sm:grid-cols-[1fr_180px]"><div className="space-y-2"><Label>Wallet name</Label><Input value={name} onChange={(event) => setName(event.target.value)} /></div><div className="space-y-2"><Label>Network</Label><select value={network} onChange={(event) => setNetwork(event.target.value as Network)} className="h-10 w-full rounded-md border border-input bg-slate-950/60 px-3 text-sm text-slate-100 outline-none">{NETWORKS.map((item) => <option key={item} value={item}>{item}</option>)}</select></div></div><div className="space-y-2"><Label>Required signatures</Label><Input min={1} max={Math.max(validSigners.length, 1)} type="number" value={threshold} onChange={(event) => setThreshold(Number(event.target.value))} /></div><div className="space-y-3"><div className="flex items-center justify-between"><Label>Signers</Label><Button variant="secondary" size="sm" onClick={() => setSigners((current) => [...current, emptySigner(`Signer ${current.length + 1}`)])}><Plus className="size-4" /> Add signer</Button></div>{signers.map((signer, index) => <div className="grid gap-2 sm:grid-cols-[160px_1fr_40px]" key={signer.id}><Input aria-label={`Signer ${index + 1} label`} value={signer.label} onChange={(event) => setSigners((current) => current.map((item) => item.id === signer.id ? { ...item, label: event.target.value } : item))} /><Input aria-label={`Signer ${index + 1} payment key hash`} aria-invalid={signer.keyHash.length > 0 && !isKeyHash(signer.keyHash)} placeholder="56-char payment key hash" value={signer.keyHash} onChange={(event) => setSigners((current) => current.map((item) => item.id === signer.id ? { ...item, keyHash: event.target.value } : item))} /><Button aria-label={`Remove signer ${index + 1}`} variant="secondary" size="icon" disabled={signers.length <= 2} onClick={() => setSigners((current) => current.filter((item) => item.id !== signer.id))}><Trash2 className="size-4" /></Button></div>)}</div><div className="grid gap-3 sm:grid-cols-2"><Button disabled={!canSave} onClick={saveCreatedWallet}><Check className="size-4" /> Save workspace</Button><Button variant="secondary" disabled={validSigners.length === 0} onClick={copyScript}>{copied ? "Copied" : "Copy script"}</Button></div></CardContent>}</Card>
      <Card className="glass-panel"><CardHeader><CardTitle className="flex items-center gap-2"><FileJson className="size-5 text-sky-300" /> {mode === "import" ? "Import preview" : "Native script preview"}</CardTitle><CardDescription>{mode === "import" ? `${importedSigners.length} unique signer key hashes discovered.` : `${validSigners.length} valid signer key hashes.`}</CardDescription></CardHeader><CardContent className="space-y-4">{mode === "import" ? <><div className="grid grid-cols-2 gap-3"><div className="rounded-lg border border-border bg-slate-950/60 p-3"><div className="text-xs text-slate-400">Payment</div><div className="font-semibold">{summarizeScript(parsedPayment.script)}</div></div><div className="rounded-lg border border-border bg-slate-950/60 p-3"><div className="text-xs text-slate-400">Stake</div><div className="font-semibold">{summarizeScript(parsedStake.script)}</div></div></div><ScriptPreview title="Payment script" script={parsedPayment.script} /><ScriptPreview title="Stake script" script={parsedStake.script} /></> : <><ScriptPreview title="Payment script" script={draftScript} /><Button variant="secondary" disabled={!canSave} className="w-full" onClick={() => downloadJson(`${slugify(name)}-payment-script.json`, draftScript)}><Download className="size-4" /> Download native-script JSON</Button></>}</CardContent></Card></section>

    <section className="grid gap-6 lg:grid-cols-[minmax(0,0.9fr)_minmax(420px,1.1fr)]"><Card className="glass-panel"><CardHeader><CardTitle className="flex items-center gap-2"><Send className="size-5 text-sky-300" /> Create transaction room</CardTitle><CardDescription>Provide transaction details and unsigned tx CBOR. Signers use the invite link to add partial witnesses.</CardDescription></CardHeader><CardContent className="space-y-4"><div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><Label>Title</Label><Input value={txTitle} onChange={(event) => setTxTitle(event.target.value)} /></div><div className="space-y-2"><Label>Amount lovelace</Label><Input value={txLovelace} onChange={(event) => setTxLovelace(event.target.value)} /></div></div><div className="space-y-2"><Label>Recipient address</Label><Input value={txRecipient} onChange={(event) => setTxRecipient(event.target.value)} placeholder="addr..." /></div><div className="space-y-2"><Label>Unsigned transaction CBOR</Label><Textarea value={txCbor} onChange={(event) => setTxCbor(event.target.value)} className="min-h-32 font-mono text-xs" placeholder="Paste unsigned tx CBOR generated for this multisig spend. Required for wallet signing." /></div><div className="space-y-2"><Label>Note</Label><Textarea value={txNote} onChange={(event) => setTxNote(event.target.value)} className="min-h-20" placeholder="Human-readable purpose for signers" /></div><div className="rounded-lg border border-border bg-slate-950/60 p-3 text-sm text-slate-300">This room will require <b className="text-sky-200">{selectedRequired}</b> of <b className="text-sky-200">{selectedSignerHashes.length}</b> signer hashes from the active {mode} workspace.</div><Button disabled={selectedSignerHashes.length === 0} onClick={createDraft} className="w-full"><Plus className="size-4" /> Create signing room</Button></CardContent></Card>
      <Card className="glass-panel"><CardHeader><CardTitle className="flex items-center gap-2"><Radio className="size-5 text-sky-300" /> Signing status</CardTitle><CardDescription>{activeDraft ? `${signatureCount(activeDraft)} of ${activeDraft.requiredSignatures} required signatures collected.` : "Create or open an invite to start collecting signatures."}</CardDescription></CardHeader><CardContent className="space-y-4">{activeDraft ? <><div className="rounded-lg border border-border bg-slate-950/60 p-4"><div className="mb-2 flex flex-wrap items-center justify-between gap-2"><div><div className="font-semibold">{activeDraft.title}</div><div className="text-sm text-slate-400">{activeDraft.walletName} · {activeDraft.network}</div></div><Badge variant={signatureCount(activeDraft) >= activeDraft.requiredSignatures ? "default" : "secondary"}>{signatureCount(activeDraft)}/{activeDraft.requiredSignatures} signed</Badge></div><div className="text-sm text-slate-400">Recipient: <span className="font-mono">{activeDraft.recipient || "not set"}</span></div><div className="text-sm text-slate-400">Amount: {activeDraft.lovelace || "not set"} lovelace</div></div><div className="grid gap-2">{activeDraft.signerKeyHashes.map((hash) => { const sig = activeDraft.signatures.find((item) => item.signerKeyHash.toLowerCase() === hash.toLowerCase()); return <div key={hash} className="flex items-center justify-between gap-3 rounded-lg border border-border bg-slate-950/50 p-3"><div className="min-w-0"><div className="truncate font-mono text-xs">{hash}</div><div className="text-xs text-slate-500">{connected?.keyHash === hash ? "connected wallet" : "signer"}</div></div><Badge variant={sig ? "default" : "outline"}>{sig ? "signed" : "pending"}</Badge></div>; })}</div><div className="grid gap-2 sm:grid-cols-2"><Button variant="secondary" onClick={() => copyInvite(activeDraft)}><Link2 className="size-4" /> Copy invite link</Button><Button disabled={!connected || !activeDraft.unsignedTxCbor} onClick={signActiveDraft}><ShieldCheck className="size-4" /> Sign with wallet</Button></div><div className="space-y-2"><Label>Signature package</Label><Textarea value={signaturePackage} onChange={(event) => setSignaturePackage(event.target.value)} className="min-h-32 font-mono text-xs" placeholder="After signing, copy this package back to the coordinator. Coordinators can paste a signer package here and import it." /><div className="grid gap-2 sm:grid-cols-2"><Button variant="secondary" disabled={!signaturePackage.trim()} onClick={copySignaturePackage}><Copy className="size-4" /> Copy package</Button><Button variant="secondary" disabled={!signaturePackage.trim()} onClick={importSignature}>Import package</Button></div></div></> : <div className="rounded-lg border border-dashed border-border bg-slate-950/40 p-8 text-center text-slate-400">No transaction room selected.</div>}</CardContent></Card></section>

    <Card className="glass-panel"><CardHeader><CardTitle>Node provider settings</CardTitle><CardDescription>Your Ogmios/Kupo/Cardano node endpoints can be stored here for the next build step: automatic UTxO lookup, tx construction, and submit.</CardDescription></CardHeader><CardContent className="grid gap-4 md:grid-cols-3"><div className="space-y-2"><Label>Kupo URL</Label><Input value={providerSettings.kupoUrl} onChange={(event) => setProviderSettings((current) => ({ ...current, kupoUrl: event.target.value }))} placeholder="https://kupo..." /></div><div className="space-y-2"><Label>Ogmios URL</Label><Input value={providerSettings.ogmiosUrl} onChange={(event) => setProviderSettings((current) => ({ ...current, ogmiosUrl: event.target.value }))} placeholder="wss://ogmios..." /></div><div className="space-y-2"><Label>Submit API URL</Label><Input value={providerSettings.nodeSubmitUrl} onChange={(event) => setProviderSettings((current) => ({ ...current, nodeSubmitUrl: event.target.value }))} placeholder="https://submit..." /></div></CardContent></Card>

    <Card className="glass-panel"><CardHeader><div className="flex items-center justify-between gap-4"><div><CardTitle>Saved wallets</CardTitle><CardDescription>Stored only in this browser. Export before switching machines.</CardDescription></div><Badge variant="secondary">{wallets.length} saved</Badge></div></CardHeader><CardContent>{wallets.length === 0 ? <div className="rounded-lg border border-dashed border-border bg-slate-950/40 p-8 text-center text-slate-400">No wallets saved yet. Import scripts or create a new policy to start.</div> : <div className="grid gap-3">{wallets.map((wallet) => <article className="grid gap-4 rounded-lg border border-border bg-slate-950/55 p-4 sm:grid-cols-[1fr_auto] sm:items-center" key={wallet.id}><div className="space-y-2"><div className="flex flex-wrap items-center gap-2"><h3 className="text-lg font-semibold text-slate-100">{wallet.name}</h3><Badge variant={wallet.imported ? "default" : "secondary"}>{wallet.imported ? "imported" : "created"}</Badge></div><p className="text-sm text-slate-400">{wallet.network} · payment {summarizeScript(wallet.paymentScript)} · stake {summarizeScript(wallet.stakeScript ?? null)} · {wallet.signers.length} signers</p></div><div className="flex flex-wrap gap-2"><Button variant="secondary" onClick={() => downloadJson(`${slugify(wallet.name)}-wallet.json`, wallet)}><Download className="size-4" /> Export</Button><Button variant="destructive" onClick={() => setWallets((current) => current.filter((item) => item.id !== wallet.id))}><Trash2 className="size-4" /> Delete</Button></div></article>)}</div>}</CardContent></Card>
    {status ? <div className="fixed bottom-4 left-1/2 z-50 max-w-[calc(100%-2rem)] -translate-x-1/2 rounded-full border border-sky-400/30 bg-slate-950/95 px-4 py-2 text-sm text-sky-100 shadow-2xl">{status}</div> : null}
  </main>;
}
